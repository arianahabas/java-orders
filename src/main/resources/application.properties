# Configuration useful for working with H2
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
#
# We set a port that is not frequently used
server.port=${PORT:2019}
#
# Feature that determines what happens when no accessors are found for a type
# (and there are no annotations to indicate it is meant to be serialized).
spring.jackson.serialization.fail-on-empty-beans=false
#
# Keeps a transaction inside of the same entity manager
# This property register on EntityManager to the current thread,
# so you will have the same EntityManager until the we request is finished.
spring.jpa.open-in-view=true
#
# What do with the schema
# drop n create table again, good for testing
# hibernate - ORM - Java <=> RDBMS
spring.jpa.hibernate.ddl-auto=create
# works with data.sql
spring.datasource.initialization-mode=never


